groups:
  - name: NodeAppAlerts
    rules:
      - alert: HighErrorRate
        expr: increase(http_requests_total{status=~"5.."}[1m]) > 5
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High 5xx rate detected"
